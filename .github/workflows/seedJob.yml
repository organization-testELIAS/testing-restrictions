name: Seed job with comment

on:
  pull_request_review_comment:
    branches: master

permissions: read-all

jobs:
  Seed-job:
    #if: ( github.event.pull_request.state == 'approved' && startsWith(github.event.comment.body,'seed') )
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        ref: ${{ github.event.pull_request.head.sha }}
    - name: Vars
      run: echo ${{ github.event.pull_request }}
    - name: Second vars 
      run: echo ${{ github.event.comment }}